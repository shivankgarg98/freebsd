

# Verify if security.mac.ipacl.enabled=0 is allowing jail to set IPv6 address

# Verify if security.mac.ipacl.ipv6=1 stops jail from setting IPv4 address

# Verify if security.mac.ipacl.rule=TODO allow jail to set certain IPv6 address

# Verify if secutiy.mac.ipacl.rule=TODO(wildcard) allow all interfaces to set certain IPv4 address


